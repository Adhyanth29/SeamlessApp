<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws the real UI; this is only the window theme. -->
    <style name="Theme.SeamlessClip" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Fully transparent, used by share/clipboard helper activities that finish immediately. -->
    <style name="Theme.SeamlessClip.Invisible" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
